# Workaround for flickering on Hyprland after electron39 update:
# stay on native Wayland (keeps clipboard/paste working) but disable
# GPU compositing, which is what flickers.
--disable-gpu-compositing
