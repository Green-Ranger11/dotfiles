{
	"tray": {
		"icon-size": 16,
		// kdeconnect-indicator reports Passive when no device is actively
		// connected; without this the icon silently vanishes.
		"show-passive-items": true,
		// "smooth-scrolling-threshold":
		"spacing": 12,
		// "reverse-direction":
		// "on-update":
		// "expand":
		// "icons":
		"cursor": true
	}
}
